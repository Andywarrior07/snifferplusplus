#include "socket.cpp"

int main() {
  // Step 1: open a socket
  int socket_file_descriptor = open_socket();

  // Step 2: inspect NICs

  // Step 3: bind to NIC

  // Step 4: recieve packages
  return 0;
}
